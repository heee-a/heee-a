# 面试讲稿 · 项目 STAR 与追问预案

> 配合各仓库 README 使用。每节按 STAR 组织：**情境 S** / 任务 T / 行动 A /
> 结果 R，后附高频追问与应答要点。建议挑 3-4 个与岗位最相关的讲深，其余一笔带过。

## 一、自我介绍 60 秒模板

> 我的方向是数据分析与 AI 应用。我建了 7 个开源仓库组成的作品集：数据方向有
> 一个 7 项目的采集分析合集和一个分析工具箱；开发方向有 FastAPI 后端、
> 从零实现的 ReAct 智能体框架和算法专题；机器学习方向有带标签泄漏对照实验的
> 端到端管线和检索方法评测。所有项目统一标准：真实公开数据、一键复现、
> CI 自动测试，结论里有统计证据，也有被数据推翻的预设叙事。

---

## 1. 空气质量分析 · 「数据推翻了我的预设」（ml-bench/02）

- **S**：想验证"北方采暖导致冬季污染更重"这个普遍认知，采集了 17 城
  2022-2024 逐小时 PM2.5（CAMS 再分析，14,977 城市-日）。
- **T**：检验南北分组与季节的差异，并量化到城市粒度。
- **A**：Mann-Whitney 检验 + 逐城冬夏比值（DJF/JJA）；全程 3 秒限速 +
  429 退避 61 秒的采集器，磁盘缓存保证重跑零请求。
- **R**：冬季峰值南北都显著（北方 p=6.9e-77，南方 p=1.1e-79）；但逐城
  比值差异远大于分组差异——重庆 1.78、深圳 1.78 高过多数北方城市，
  北方的沈阳只有 0.98。**"北方采暖更糟"的二分叙事在这份数据上不成立，
  README 如实记录了被推翻的过程。**

**追问预案**：
- 「再分析数据可信吗？」→ CAMS 是 ECMF 再分析，趋势与相对比较可信；
  绝对值有系统偏差（北京 75 高于站点实测口径），README 已声明"绝对值慎用"。
- 「为什么不用站点数据？」→ 全国站点历史数据无公开 API；项目目标是
  方法论演示，口径限制已写进局限一节。
- 「多重比较问题？」→ 两个独立检验（南/北），如需严格可 Bonferroni 校正，
  α=0.025 下结论不变。

## 2. 机器学习端到端 · 标签泄漏对照实验（ai-lab/01）

- **S**：想演示"特征工程 + 交叉验证"的标准流程，选了"预测国家收入等级"。
- **T**：写完第一版后发现：收入等级就是世界银行按人均收入阈值划的——
  把人均GDP 放进特征等于把答案喂给模型。
- **A**：设计**对照实验**：防泄漏特征（寿命/CO2/城市化）vs 含 GDP 特征，
  同一 5 折分层交叉验证各跑一遍；随机森林 + 逻辑回归对比；置换重要性解释。
- **R**：防泄漏 68.3%，泄漏对照 76.6%——10 个百分点的"提升"纯属信息泄漏；
  另一个教学点：训练集内 96.9% vs 交叉验证 68.3%，192 个小样本必须以 CV 为准。

**追问预案**：
- 「怎么发现泄漏的？」→ 问自己"这个特征和标签的定义关系"：标签由收入阈值
  定义，收入类特征天然与标签构成循环；
- 「还有什么泄漏类型？」→ 时间泄漏（用未来数据）、目标编码泄漏、
  测试集标准化泄漏（本项目用 Pipeline 把 scaler 收进折内）；
- 「68% 是不是太低？」→ 任务本身难度如此（相邻等级边界模糊），混淆矩阵
  显示误判集中在相邻等级，业务上可接受。

## 3. 数据采集基建 · 与真实限流交手（data-portfolio）

- **S**：多个项目需要从公开 API 采集数据，网络环境复杂（共享出口 IP）。
- **A**：写了一个统一的礼貌采集基座：磁盘缓存（同 URL 只请求一次）、
  最小间隔限速、指数退避、尊重 Retry-After。
- **R**：三个真实战例全部自动恢复——Open-Meteo 每分钟限流（等 61s）、
  GitHub 搜索 API 1000 条硬上限（第 11 页 422，优雅终止并记录边界）、
  World Bank CO2 指标码归档（按报错换新码）。

**追问预案**：
- 「怎么水平扩展？」→ 单机缓存版够用时不过度设计；真要规模化会换
  消息队列 + 工作池，但礼貌采集（限速）仍是前提；
- 「数据更新怎么处理？」→ 快照随仓库提交 + 采集脚本可重跑，当前规模
  不需要增量管道（诚实说"还没到需要的时候"）。

## 4. RAG 检索问答 · 用评测集驱动迭代（ai-lab/02）

- **S**：做了个 RAG 问答，意识到"能跑"和"答得准"是两回事。
- **T**：给检索建评测：12 条中文测试问题（期望文档 + 关键词），
  指标 recall@3 / MRR。
- **A**：三轮迭代全记录——v1 一级标题粗分块（50%）→ v2 多级标题切分 +
  层级路径并入正文（67%）→ v3 标题命中加权（**83%**，MRR 0.75）；
  BM25 从零实现（Okapi 公式 + jieba）。
- **R**：每一步提升都有评测数字背书；剩余 2 个未命中的归因也写了
  （词面匹配局限，升级方向是语义向量检索）。

**追问预案**：
- 「为什么不用向量数据库/embedding？」→ 语料只有 81 块，先建评测集
  再谈技术选型；ml-bench/03 的对照实验显示小语料上 BM25（83%）胜过
  LSA（67%）——不迷信"向量更高级"；
- 「幻觉怎么处理？」→ 系统提示限制"仅依据资料回答"+ 引用编号标注；
  严格幻觉评测是列入 roadmap 的下一步。

## 5. OCR 毕业度计算 · 算法与工程结合（yanyun-gradecalc）

- **S**：手机拍游戏装备屏幕算词条"毕业度"，照片必然是斜的。
- **A**：RapidOCR 取出文本框坐标后，**保序动态规划配对**：数值按 x 聚类
  成列、名字池全保留、类型约束（百分比词条只能配百分比数值）+ 偏移线性
  漂移模型（最小二乘）修正斜拍错位。
- **R**：4 张斜拍照片全部配对正确；我自己目视都读错行的样本，算法配对了。
  还发现并自动排除了"基础属性行"（数值超满值 1.3 倍的行）。

**追问预案**：
- 「为什么不训练一个检测模型？」→ 无标注数据，几何+类型约束的规则
  在这个规模下已 100% 准确，先建评测集再考虑模型（与 RAG 项目同哲学）；
- 「OCR 错字怎么办？」→ 词条名走别名模糊匹配（difflib），未知词条
  保留原文并标注"未识别"，不污染评分。

## 6. 统计检验进 ML 对比（ml-bench/01）

- **S**：对比 5 个模型在 3 个经典数据集上的准确率。
- **A**：RepeatedStratifiedKFold（5 折 × 2 次），全部模型共享同一批折
  （配对前提），前两名与首尾各做一组 Wilcoxon 符号秩检验。
- **R**：头部模型差异全部不显著（p=0.83/0.21/0.63），首尾差异全部显著
  （p≤0.008）。结论：小数据集上"换成 XX 模型提升 0.1%"没有统计证据，
  线性/核方法仍是最强基线。

**追问预案**：
- 「为什么要显著性检验？」→ 防止把随机波动当提升；配对设计消除折间方差；
- 「你自己的项目里有 p-hacking 吗？」→ 检验一是负结果且如实报告了
  （空气质量项目），并说明了多重比较的校正口径。

---

## 二、快速索引（一句话版）

| 项目 | 一句话 | 亮点数字 |
|---|---|---|
| data-portfolio | 7 个采集分析全链路项目 | 39,456 行零缺失；采集零人工干预 |
| bda-toolkit | 一条命令出 16 Sheet 分析报告 | demo 数据 + 样例报告入库 |
| ai-lab/01 | 防泄漏对照实验 | 泄漏虚高 +10pp |
| ai-lab/02 | 带评测的 RAG | recall@3 50%→83% |
| dev-portfolio/software | FastAPI 分层后端 | 12-factor 配置外置 |
| dev-portfolio/ai-agents | ReAct 从零实现 | 工具沙箱 + 自我纠错实测 |
| dev-portfolio/algorithms | 25+ 算法 + 基准 | n=8000 时 O(n²) 慢百倍 |
| ml-bench/01 | 统计检验进模型对比 | digits 98.3%，负结果如实报告 |
| ml-bench/03 | BM25 vs LSA | 小语料 BM25 胜出 |
| yanyun-gradecalc | OCR + 几何配对算法 | 斜拍照片 100% 配对 |

## 三、被问「你的不足」时的答案

1. **数据工程**：项目规模下没引入 Airflow/消息队列——刻意保持简单，
   但清楚什么时候该上（数据源 >5 个、需要小时级增量时）；
2. **深度学习**：作品集以经典方法为主，深度模型经验是下一步补的方向；
3. **大规模**：千万行级分布式处理经验有限，pandas/SQL 的优化经验来自
   真实项目的百万行内场景。
